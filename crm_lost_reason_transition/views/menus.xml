<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="action_crm_lost_reason_transition" model="ir.actions.act_window">
        <field name="name">Loss Reason Transitions</field>
        <field name="res_model">crm.lost.reason.transition</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p
            >Configure where lost leads should be redirected, based on their reason and current stage.</p>
        </field>
    </record>

    <menuitem
        id="menu_crm_lost_reason_transition"
        name="Lost Lead Transitions"
        parent="crm.menu_crm_config_lead"
        sequence="7"
        action="action_crm_lost_reason_transition"
    />

</odoo>
