This Odoo module enhances the CRM app by introducing configurable transitions for lost
leads. It allows sales managers to define redirection rules based on the loss reason and
the current stage of the lead.

When a lead is marked as lost, the system automatically:

* Redirects it to a specific sales team
* Moves it to a specific recovery stage (pipeline column)

This ensures that each loss reason is handled by the appropriate recovery workflow.
