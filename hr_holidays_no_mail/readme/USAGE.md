When a new vacation allocation is created, the system will not send automatic email notifications to the user or any other party involved.

Similarly, when updating an existing vacation allocation, the system disables automatic email notification.

The same behavior applies when a vacation request is created or changed.
