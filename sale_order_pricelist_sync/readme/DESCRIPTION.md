This Odoo module ensures that when a quotation template is selected on a sale order, the
pricelist defined in the template is automatically applied to the order.

This behavior overrides the customer's default pricelist to enforce consistent pricing
based on the selected quotation template.

It is useful for campaigns like "Black Friday", where special templates require specific
pricelists regardless of customer configuration.
