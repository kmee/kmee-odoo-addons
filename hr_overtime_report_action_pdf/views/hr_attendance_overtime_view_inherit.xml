<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="action_server_print_pdf" model="ir.actions.server">
        <field name="name">Exportar PDF</field>
        <field name="model_id" ref="model_hr_attendance_overtime" />
        <field name="binding_model_id" ref="model_hr_attendance_overtime" />
        <field name="state">code</field>
        <field
            name="code"
        >action = env.ref('hr_overtime_report_action_pdf.action_report_overtime').report_action(env['hr.attendance.overtime'].browse(env.context.get('active_ids', [])))</field>
    </record>
</odoo>
